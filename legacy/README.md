# Legacy Streamlit UI

Предыдущая версия интерфейса на Streamlit. Основной продукт — React + API в корне репозитория.

```bash
pip install -r requirements-streamlit.txt
streamlit run streamlit_app.py
```

Файл `streamlit_app.py` — копия прежнего `app.py` на момент миграции.
